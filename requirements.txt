# ─────────────────────────────────────────────
# FinSight AI — Production Dependencies
# ─────────────────────────────────────────────

# ── HTTP & API clients ────────────────────────
requests==2.32.3
httpx==0.27.0
aiohttp==3.9.5

# ── Data processing ───────────────────────────
pandas==2.2.2
numpy==1.26.4
pyarrow==16.1.0
delta-spark==3.2.0                 # Delta Lake Python bindings

# ── PySpark (local dev — Databricks runtime on cloud) ──
pyspark==3.5.1

# ── Azure SDK ─────────────────────────────────
azure-storage-file-datalake==12.17.0
azure-identity==1.17.1
azure-keyvault-secrets==4.8.0
azure-mgmt-resource==23.1.1

# ── Databricks ────────────────────────────────
databricks-sdk==0.28.0
databricks-feature-engineering==0.6.0
mlflow==2.14.1

# ── ML / AI ───────────────────────────────────
scikit-learn==1.5.0                # Isolation Forest
xgboost==2.0.3                     # Trend classifier
openai==1.35.13                    # GPT-4o-mini sentiment
faiss-cpu==1.8.0                   # Vector similarity search
sentence-transformers==3.0.1       # News embedding model

# ── FastAPI stack ─────────────────────────────
fastapi==0.111.0
uvicorn[standard]==0.30.1
pydantic==2.7.4
pydantic-settings==2.3.4

# ── Config & env ─────────────────────────────
python-dotenv==1.0.1
pyyaml==6.0.1

# ── Logging & monitoring ─────────────────────
structlog==24.2.0
prometheus-fastapi-instrumentator==7.0.0
